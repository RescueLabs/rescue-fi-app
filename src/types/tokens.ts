export interface ITokenMetadata {
  image?: string;
  type: string;
  info: string;
  symbol: string;
  amount: string;
  decimals: number;
  toEstimate: {
    from: `0x${string}`;
    to: `0x${string}`;
    data: `0x${string}`;
  };
}
